{{yield}}
<div class="container">
  <div class="row">
    {{#each watchList.items as |item|}}
      <div class="col-sm-12">
        <p>{{item.name}}</p>
        <img class="product-img" src="{{item.image}}">
        <p>{{item.salePrice}}</p>
      </div>
    {{/each}}
  </div>
</div>
